name: sreerajp_journal_vault
description: "A private, encrypted journal with attachments, search, and insights."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.5.7+14

environment:
  sdk: ^3.12.2
  flutter: ">=3.44.8"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Required by the generated AppLocalizations, and used for date and number
  # formatting. See l10n.yaml and lib/l10n/.
  intl: ^0.20.2

  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.1
  riverpod: ^3.2.1
  drift: ^2.29.0
  path: ^1.9.1
  path_provider: ^2.1.5
  # SQLCipher, not plain SQLite. The vault is encrypted at rest with a key held
  # in the Android Keystore — see lib/core/database/encrypted_database_opener.dart.
  # The native library is chosen by the `hooks:` block at the bottom of this file.
  #
  # sqlite3_flutter_libs and drift_flutter must NOT come back: they bundle a
  # second copy of libsqlite3, and the vault could end up written in plain form
  # with no error at all. See docs/dependencies.md.
  sqlite3: ^3.5.0
  flutter_quill: ^11.5.0
  file_picker: ^11.0.2
  cryptography: ^2.7.0
  crypto: ^3.0.6
  mime: ^2.0.0
  open_filex: ^4.7.0
  archive: ^4.0.7
  shared_preferences: ^2.5.3
  pdfrx: ^2.4.7
  just_audio: ^0.10.5
  permission_handler: ^12.0.1
  package_info_plus: ^9.0.0
  device_info_plus: ^12.1.0
  table_calendar: ^3.2.0
  record: ^7.1.1
  speech_to_text: ^7.0.0
  uuid: ^4.5.1
  local_auth: ^3.0.2
  local_auth_android: ^2.0.9
  logger: ^2.4.0
  image_picker: ^1.2.3
  image_cropper: ^12.2.1
  google_mlkit_text_recognition: ^0.17.1
  # Pure-Dart image decode/resize/filters, used to prepare photos before OCR.
  # No networking dependency - see docs/dependencies.md.
  image: ^4.5.4
  qr_flutter: ^4.1.0
  mobile_scanner: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  build_runner: ^2.10.3
  # Test-only. `local_auth` does not re-export LocalAuthPlatform, which
  # biometric_authenticator_test.dart swaps out to fake the device.
  local_auth_platform_interface: ^1.1.0
  drift_dev: ^2.29.0
  flutter_launcher_icons: ^0.14.4

flutter_launcher_icons:
  android: "ic_launcher"
  ios: true
  remove_alpha_ios: true
  image_path: "assets/icon/icon.png"
  min_sdk_android: 28
  adaptive_icon_background: "#1E1B4B"
  adaptive_icon_foreground: "assets/icon/icon_foreground.png"
  web:
    generate: false
  windows:
    generate: false
  macos:
    generate: false

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Turns on the localization generator. It reads l10n.yaml and writes
  # AppLocalizations into lib/l10n/. Required by the engineering standard
  # section 8.1 for every app, including single-language ones.
  generate: true

  # About-screen config, required by guideline.md section 1.3. Without this
  # line the JSON is not bundled and the app silently falls back to defaults.
  assets:
    - assets/config/
    # Malayalam fonts for HTML and PDF export. Loaded with rootBundle and
    # embedded into the exported page as base64 @font-face data URIs, so the
    # page stays self-contained and offline — see export_html_builder.dart.
    #
    # Listed under `assets`, NOT under `fonts`, on purpose: Flutter itself
    # never renders with them. Only the export WebView does, and it needs the
    # raw bytes.
    #
    # Noto Sans Malayalam, SIL Open Font Licence 1.1. The licence text ships
    # beside the fonts in OFL-Noto.txt, which the licence requires.
    - assets/fonts/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# Build hooks. `package:sqlite3` compiles or downloads the native library
# itself; this picks the SQLCipher build over plain SQLite, on every platform
# including the test runner. It is what makes the vault encrypted at rest.
#
# Changing `source` here silently changes whether journal text is encrypted.
# Do not touch it without reading docs/security.md.
hooks:
  user_defines:
    sqlite3:
      source: sqlcipher
